// Use draws generated by custom code and upload to the Epi database
clear all
set more off

adopath + "FILEPATH"

get_best_model_versions, entity(modelable_entity) ids(3075 3076) clear
levelsof model_version_id, local(models)

quietly do FILEPATH/save_results.do
save_results, modelable_entity_id(1810) description("Combination of low and high models; `models'; custom CSMR") in_dir("FILEPATH/me_1810") metrics(5 6) mark_best(yes)

